<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Resolver reto</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
    <h1>Resolver reto: {{reto.titulo}}</h1>
    <p>{{reto.descripcion}}</p>
    <form action="/resolver/{{reto.id}}" method="post">
        <label>Flag:<br><input type="text" name="flag" required></label>
        <button type="submit">Comprobar</button>
    </form>
    {{#resultado}}
    <p style="font-weight:bold;color:{{#correcto}}green{{/correcto}}{{^correcto}}red{{/correcto}};">{{resultado}}</p>
    {{/resultado}}
    <a href="/">Volver</a>
</body>

</html>
