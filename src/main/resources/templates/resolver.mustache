<!DOCTYPE html><!DOCTYPE html>

<html lang="es"><html lang="es">

<head><head>

    <meta charset="UTF-8">    <meta charset="UTF-8">

    <title>Resolver reto</title>    <title>Resolver reto</title>

    <link rel="stylesheet" href="/css/styles.css">    <link rel="stylesheet" href="/css/styles.css">

</head></head>

<body><body>

    <h1>Resolver reto: {{reto.titulo}}</h1>    <h1>Resolver reto: {{reto.titulo}}</h1>

    <p>{{reto.descripcion}}</p>    <p>{{reto.descripcion}}</p>

    <form action="/resolver/{{reto.id}}" method="post">    <form action="/resolver/{{reto.id}}" method="post">

        <label>Flag: <input type="text" name="flag" required></label>        <label>Flag: <input type="text" name="flag" required></label>

        <button type="submit">Comprobar</button>        <button type="submit">Comprobar</button>

    </form>    </form>

    {{#resultado}}    {{#resultado}}

    <p><b>{{resultado}}</b></p>    <p><b>{{resultado}}</b></p>

    {{/resultado}}    {{/resultado}}

    <a href="/">Volver</a>    <a href="/">Volver</a>

</body></body>

</html></html>
